# frozen_string_literal: true

RSpec.describe Rubocop::OracleAdapter do
  it "has a version number" do
    expect(Rubocop::OracleAdapter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
